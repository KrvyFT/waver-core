# waver-core

Graph IR、模块目录（`ModuleDesc`）、编译调度与无锁 `ParamCell`。

**请在伞仓中开发**：<https://github.com/KrvyFT/waver>（本目录为 `crates/waver-core`）。  
独立远程仅作源码镜像；`Cargo.toml` 依赖 workspace 继承，单独 clone 无法直接 `cargo build`。

同步说明见伞仓 [doc/repos.md](https://github.com/KrvyFT/waver/blob/master/doc/repos.md)。
